{
  "name": "fee-market",
  "private": true,
  "scripts": {
    "clean": "lerna clean",
    "boot": "lerna bootstrap",
    "format": "prettier '**/*.{js,jsx,ts,tsx,json}' --write",
    "prepare": "husky install"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "husky": "^8.0.0",
    "lerna": "^5.5.1",
    "lint-staged": "^13.0.3",
    "prettier": "2.7.1"
  }
}
